let pairs = range 1 2049 |> map { x => [text x, x] } |> collect
let left = record pairs
let right = record (reverse pairs)

fn loop n =
  if n == 0 then true
  else if left == right then loop (n - 1)
  else false

if loop 128 then () else raise (error "Benchmark" "record result")
