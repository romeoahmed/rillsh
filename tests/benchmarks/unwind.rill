fn chain n source =
  if n == 0 then source else chain (n - 1) (map identity source)

do {
  let earlier = chunks (bytes [])
  let outcome = attempt { () =>
    let abandoned = chain 8192 earlier
    raise (error "Benchmark" "unwind")
  }
  match outcome of {
    Result.Err {error} => if error.kind == "Benchmark" then () else exit 42,
    _ => exit 42
  }
}
