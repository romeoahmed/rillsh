import "std:core" as core
import "std:fs" as fs
import "std:json" as json
import "std:process" as process
import "std:seq" as seq
import "std:text" as text

export {
  Option: core.Option,
  Result: core.Result,
  Control: core.Control,
  Error: core.Error,
  some: core.some,
  ok: core.ok,
  err: core.err,
  error: core.error,
  attempt: core.attempt,
  raise: core.raise,
  identity: core.identity,
  compose: core.compose,
  add: core.add,
  subtract: core.subtract,
  multiply: core.multiply,
  divide: core.divide,
  equal: core.equal,
  less: core.less,
  div: core.div,
  rem: core.rem,
  int: core.int,
  float: core.float,
  text: core.text,
  record: core.record,
  extend: core.extend,
  to_record: core.to_record,
  lookup: core.lookup,
  entries: core.entries,
  bytes: core.bytes,
  path: core.path,

  files: fs.files,
  glob: fs.glob,
  read_text: fs.read_text,
  read_text_with: fs.read_text_with,
  join_path: fs.join_path,
  basename: fs.basename,
  dirname: fs.dirname,

  from_json: json.from_json,
  from_json_with: json.from_json_with,
  to_json: json.to_json,
  to_json_with: json.to_json_with,

  length: seq.length,
  concat: seq.concat,
  reverse: seq.reverse,
  take: seq.take,
  drop: seq.drop,
  fold: seq.fold,
  fold_until: seq.fold_until,
  each: seq.each,
  map: seq.map,
  filter: seq.filter,
  filter_map: seq.filter_map,
  find: seq.find,
  any: seq.any,
  all: seq.all,
  sum: seq.sum,
  sort_by: seq.sort_by,
  sort_by_with: seq.sort_by_with,
  chunks: seq.chunks,
  items: seq.items,
  unfold: seq.unfold,
  range: seq.range,
  collect: seq.collect,
  collect_with: seq.collect_with,
  collect_bytes: seq.collect_bytes,
  collect_bytes_with: seq.collect_bytes_with,
  close: seq.close,

  byte_length: text.byte_length,
  scalars: text.scalars,
  encode_utf8: text.encode_utf8,
  decode_utf8: text.decode_utf8,
  starts_with: text.starts_with,
  ends_with: text.ends_with,
  lines: text.lines,
  lines_with: text.lines_with,
  display_path: text.display_path,
  split: text.split,
  join: text.join,
  trim: text.trim,
  parse_int: text.parse_int,
  parse_float: text.parse_float,

  Termination: process.Termination,
  Completion: process.Completion,
  JobReport: process.JobReport,
  command: process.command,
  pipe: process.pipe,
  with_cwd: process.with_cwd,
  with_env: process.with_env,
  accept_exit: process.accept_exit,
  run: process.run,
  execute: process.execute,
  stdin: process.stdin,
  print: process.print,
  write_text: process.write_text,
  write_bytes: process.write_bytes,
  start: process.start,
  wait: process.wait,
  check: process.check,
  fg: process.fg,
  bg: process.bg,
  cancel: process.cancel,
  jobs: process.jobs,
  cd: process.cd,
  pwd: process.pwd,
  set_env: process.set_env,
  get_env: process.get_env,
  unset_env: process.unset_env,
  args: process.args,
  exit: process.exit,
  exit_force: process.exit_force,
  capture: process.capture,
  capture_with: process.capture_with,
  stream: process.stream,
  through: process.through,
  write_stdout: process.write_stdout
}
